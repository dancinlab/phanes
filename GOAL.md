# GOAL — phanes 의 한 문장

```
/goal hexa-lang 의 OUROBOROS 발견 엔진(hexa kick)을, 실제 기업이 seed 한 줄로
돌려 검증·이력추적된 발견 카탈로그(그들만의 echoes)를 얻는 web SaaS 로 완성한다.
```

> **한 문장 (canonical)**: hexa-lang 의 자율 발견 엔진 **`hexa kick`**(OUROBOROS:
> smash→free→absolute→meta→hyper→resonance, saturation 까지 라운드 반복 +
> 라운드별 honesty 게이트)을, 실제 기업이 **web 으로** 돌려 **검증·provenance
> 추적된 발견 카탈로그**(그들만의 echoes)를 받는 SaaS 로 완성한다 — `echoes-
> experience` 처럼 작게·정직하게·실제 출시.

---

## 무엇이 아닌가 (NOT)

- hexa kick 엔진 fork 아님 — phanes 는 다운스트림 소비자, 엔진 갭은 hexa-lang `inbox/patches/`
- "당신 프로젝트 자동완성" 아님 — honesty gate 는 advisory, hard stop 은 saturation 뿐 (over-claim 금지)
- design-first 아님 — 제품 scope 는 결정 게이트로 확정 (design.md)
- 가짜 진행 아님 — 미달은 미달로 기록 (hexa-lang g3 · LATTICE_POLICY)

## 무엇인가 (IS)

```
hexa kick (OUROBOROS, hexa-lang upstream · cycle h36 COMPLETE)
  → phanes control plane (auth · tenant 격리 · job queue · metering)
  → phanes compute plane (Linux fleet · HEXA_VAL_ARENA=0 · per-job overlay)
  → per-tenant 검증 발견 카탈로그 (private echoes · provenance · 내보내기)
= "실제 기업이 안전하게 hexa kick 을 돌려 검증된 발견을 받는다" 의 web 화
```

## 현재 정직한 위치 (g3 — over-claim 금지)

**Scaffold 단계. 제품 scope 미결 (Decision 1 OPEN).** repo 생성·정체성·거버넌스
확정 (name=Phanes, downstream invariant, honest-scope). scope(A/B/C)·배포형태·
GitHub org·라이선스·멀티테넌트 격리방식 = 결정 게이트 대기.

## cross-link

- `design.md` — 결정 게이트 로그 (Decision 1 scope OPEN · Decision 2 name=Phanes DECIDED)
- `AGENTS.tape` — 정체성 + 거버넌스 (downstream · honest-scope · name-risk)
- hexa-lang `compiler/drill/drill.hexa` · `compiler/PLAN.md` §"hexa kick COMPLETE — cycle h36" — 업스트림 엔진 SSOT
- hexa-lang `gate/hexa_url_modules.ai.md` — 기존 `hexa://kick?topic=` 원격 디스패치 (proto-SaaS 표면)

---

## Log

- **2026-05-19** — repo scaffold. `~/core/phanes` git init (main). Decision 2
  (name = Phanes) DECIDED with trademark-risk on record; Decision 1 (product
  scope A/B/C) OPEN — next gate. North-star fixed; IS clause to be
  concretized once scope is picked. SSOT for progress/decisions = `design.md`.
- **2026-05-19** — Decision 4 DECIDED: pushed to `github.com/dancinlab/phanes`
  (**private**, `origin`/main). Trademark clearance deferred-but-owed before
  any public launch (`@D g_name_risk`). Decision 1 (scope) still OPEN.
