# DESIGN — phanes

Live design pointer. Current spec / governance / identity = `project.tape`
(linked as `CLAUDE.md`). Decision-gate history (Decisions 1–24,
chronologically logged) = [`DESIGN.log.md`](DESIGN.log.md).

Build path = [`ROADMAP.md`](ROADMAP.md); product status = [`GOAL.md`](GOAL.md).
